import type { BarrierStatus } from '../domain/types'
import { StatusBadge } from './StatusBadge'
const stages = [{key:'observed',label:'Observed',actor:'Auditor'},{key:'validated',label:'Validated',actor:'Designated reviewer'},{key:'prioritised',label:'Prioritised',actor:'Facilities'},{key:'assigned',label:'Assigned',actor:'Facilities'},{key:'awaiting_verification',label:'Fixed / awaiting verification',actor:'Repair owner'},{key:'verified',label:'User verified',actor:'Independent verifier'}] as const
const statusIndex: Record<BarrierStatus,number> = { observed:0,validated:1,prioritised:2,assigned:3,rework_required:3,awaiting_verification:4,verified:5 }
export function LifecycleRail({status}:{status:BarrierStatus}) { const currentIndex=statusIndex[status]; return <nav className="lifecycle" aria-label="Demo journey"><p className="eyebrow">Evidence chain</p><h2>Demo journey</h2><ol>{stages.map((stage,index)=>{const state=index<currentIndex?'Complete':index===currentIndex?'Current':'Upcoming';return <li key={stage.key} data-state={state.toLowerCase()}><a href={`#stage-${stage.key}`} aria-current={index===currentIndex?'step':undefined}><span className="stage-number" aria-hidden="true">{index+1}</span><span><strong>{stage.label}</strong><small>{stage.actor}</small></span><StatusBadge tone={state==='Complete'?'success':state==='Current'?'info':'neutral'}>{state}</StatusBadge></a>{status==='rework_required'&&index===3?<StatusBadge tone="danger">Action required</StatusBadge>:null}</li>})}</ol></nav> }
